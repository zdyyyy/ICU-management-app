# OpenAI API Key (chat + embeddings for RAG)
OPENAI_API_KEY=your_openai_api_key_here

# Optional RAG: top-k chunks from knowledge/*.md (default 4)
# RAG_TOP_K=4
# RAG_EMBEDDING_MODEL=text-embedding-3-small

# Server Port
PORT=3000
NODE_ENV=development